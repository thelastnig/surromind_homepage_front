import React from 'react';
import AIStudioEventContainer from '../containers/AIStudioEventContainer';

const AIStudioEventPage = (props) => {
  return (
    <AIStudioEventContainer {...props}/>
  )
}

export default AIStudioEventPage;