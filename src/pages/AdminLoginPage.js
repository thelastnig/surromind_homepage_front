import React from 'react';
import AdminLoginContainer from '../containers/AdminLoginContainer';

const AdminLoginPage = (props) => {
  return (
    <AdminLoginContainer {...props}/>
  )
}

export default AdminLoginPage;