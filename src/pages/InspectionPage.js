import React from 'react';
import InspectionContainer from '../containers/InspectionContainer';

const InspectionPage = (props) => {
  return (
    <InspectionContainer {...props}/>
  )
}

export default InspectionPage;